# doc_classifier
The Document Classification Detector is a Python-based pre-processing module designed to automatically detect document classification levels (e.g., Secret, Restricted, Internal, No Classification, None) from multiple sources.
